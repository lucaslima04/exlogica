#include <stdio.h>

int main()
{
    int indice=13, soma=0, k=0;
    
    while(k < indice)
    {
        k = k + 1;
        soma = soma + k;
    }
    printf ("Soma = %d \n" , soma); //Nesse caso o resultado da soma ao final do código será igual a 91
    return 0;
}
